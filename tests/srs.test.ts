import { describe,expect,it } from "vitest";
import { reviewSrs } from "../src/core/srs.js";

describe("SRS",()=>{it("applies deterministic review schedules",()=>{const base={status:"NEW" as const,ease:2.5,intervalDays:0,repetitions:0,correctCount:0,incorrectCount:0},at=new Date("2026-01-01T00:00:00.000Z");expect(reviewSrs(base,"AGAIN",at)).toMatchObject({status:"LEARNING",ease:2.3,intervalDays:1,repetitions:0,incorrectCount:1,nextReviewAt:"2026-01-02T00:00:00.000Z"});expect(reviewSrs(base,"GOOD",at)).toMatchObject({status:"LEARNING",intervalDays:1,repetitions:1,correctCount:1});expect(reviewSrs(base,"EASY",at)).toMatchObject({intervalDays:4,ease:2.65});});});

describe("SRS invariants",()=>{it("keeps intervals and repetitions non-negative and EASY beyond GOOD",()=>{const state={status:"REVIEW" as const,ease:2.5,intervalDays:10,repetitions:4,correctCount:4,incorrectCount:1},at=new Date("2026-01-01T00:00:00.000Z"),again=reviewSrs(state,"AGAIN",at),hard=reviewSrs(state,"HARD",at),good=reviewSrs(state,"GOOD",at),easy=reviewSrs(state,"EASY",at);for(const result of[again,hard,good,easy]){expect(result.intervalDays).toBeGreaterThanOrEqual(0);expect(result.repetitions).toBeGreaterThanOrEqual(0);expect(Number.isNaN(Date.parse(result.nextReviewAt))).toBe(false);}expect(easy.intervalDays).toBeGreaterThan(good.intervalDays);expect(good.intervalDays).toBeGreaterThan(hard.intervalDays);expect(again.status).not.toBe("MASTERED");});});
